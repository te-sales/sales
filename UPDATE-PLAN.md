# UPDATE-PLAN — แผนอัพเดทที่รอเจ้าของเคาะ

> **ที่มา:** ประเมินไฟล์ "ชุดคำสั่ง Claude Code — TE Sales Dashboard (P1–P11)" (ลงวันที่ 26 ก.ค. 2569) เทียบกับสถานะจริงของระบบ
> **สถานะไฟล์นี้:** แผนที่ *รอเคาะ* — **ยังไม่ลงมือ** · ต้องให้เจ้าของเลือกก่อนเสมอ (กติกาเดียวกับ WishtoHave.md)
> **แบ่งหน้าที่กับไฟล์อื่น (อย่าเขียนซ้ำ):** `CLAUDE.md` = แผน/สเปคของที่ทำแล้ว · `WishtoHave.md` = ดัชนีสิ่งที่อยากได้ (ชี้มาไฟล์นี้) · ไฟล์นี้ = รายละเอียดแผนอัพเดตชุด P1–P11 + ดีไซน์ Quick-log

---

## สรุปสถานะ P1–P11 (เทียบของจริง)

| # | เรื่อง | สถานะจริง | ทำต่อไหม |
|---|---|---|---|
| **P1** | Archive (แสดง/ซ่อนงานจบ) | ✅ เสร็จแล้ว **Step 2.5** (`is_active` ครบทั้ง schema/adapter/dashboard) | ❌ ทำแล้ว |
| **P2** | พิมพ์การ์ด Potential (Book 3) | ✅ เสร็จแล้ว **Step 3.9** (`print.css` + `formprint.js`) | ❌ ทำแล้ว · P2 สั่งฝัง hex เอง = ขัดกฎ print.css |
| **P3** | แถบ Supplier (B7 + F9) | 🚫 เจ้าของสั่งถอดถาวร (23 ก.ค.) · `project_suppliers` = ตัดถาวร | ❌ ไม่ทำ (ทำได้เมื่อเจ้าของกลับคำ) |
| **P4** | AI Intake (🤖 AI Import) | ✅ เสร็จแล้ว **3.5 + 3.8** · ของจริงล้ำกว่า (P4 ยังอ้าง Obsidian/Notion ที่ถอดไปแล้ว) | ❌ ทำแล้ว |
| **P5** | AI Paste (วางข้อความ→AI กรอก) | ⚠️ ส่วนใหญ่ทำแล้ว (source "ข้อความคัดลอก" + AI บันทึก v0.41–0.42) | ✅ **เฉพาะ regex สกัดออฟไลน์** (ใหม่) |
| **P6** | Export/Backup ทุก module | ✅ เสร็จแล้ว **Step 3.6** (`buildBackup`/`exportAll`) | ❌ ทำแล้ว |
| **P7** | แจ้งเตือน Telegram | 🆕 ยังไม่มี | ✅ คุ้ม (ต้องตั้ง server + เคาะช่องทาง) |
| **P8** | Weekly Digest (สรุปจันทร์) | 🆕 ยังไม่มี | ✅ คุ้ม (ทำต่อจาก P7) |
| **P9** | Win/Loss Analysis | ⚠️ เก็บ `result_because` + ปุ่ม Success/Miss แล้ว · ยังไม่มีวิเคราะห์ | ✅ **คุ้มสุด** ต่อยอดของเดิม |
| **P10** | G-Lead feed (tender leads) | 🆕 ยังไม่มี (G-LEAD เป็นแค่ลิงก์ใน `lead_sources`) | ✅ คุ้ม (ต้องชัดเรื่องแหล่งข้อมูลก่อน) |
| **P11** | Quick-log มือถือ | ⚠️ ป็อปอัปบันทึกเร็วต่อ record มีแล้ว | ✅ **ดีไซน์ใหม่ตามที่ขอ** (ดูด้านล่าง) |

---

## 🟩 คุ้มทำต่อ — เรียงลำดับที่แนะนำ

### 1) P9 — Win/Loss Analysis  ⭐ แนะนำเริ่มตัวแรก
- **ทำอะไร:** ตอนเปลี่ยนสถานะ Pending เป็น won/lost → เด้ง modal ถามข้อมูลเพิ่ม
  (won: เหตุผลชนะ + คู่แข่งที่ชนะ · lost: แพ้ให้ใคร + เหตุผล[ราคา/สเปก/ความสัมพันธ์/เวลา/อื่น] + ราคาเราเทียบผู้ชนะ)
  → หน้า "Win/Loss" บน dashboard: อัตราชนะรวม/รายทีม · top-5 เหตุผลแพ้ · คู่แข่งที่เจอบ่อย
- **กระทบโครงสร้าง:** เพิ่มตารางเล็ก `win_loss` (FK → pending) + list เหตุผลใน `app_settings` · ต่อยอด `result_because`/`stage`/`competitors` ที่มีอยู่แล้ว
- **migration:** ไม่ต้อง · **ติดอะไรก่อน:** ไม่มี · **ราคาถ้าเลื่อน:** ถูกเท่าเดิม (แต่ยิ่งเก็บ won/lost ไปเยอะโดยไม่มีช่องเหตุผล ยิ่งเสียข้อมูลวิเคราะห์ย้อนหลัง)
- **ขนาด:** M · frontend เป็นหลัก + ตารางเดียว ไม่ต้อง server

### 2) P5 — regex สกัดออฟไลน์ (เฉพาะส่วนใหม่)
- **ทำอะไร:** ก่อนพึ่ง AI ให้ regex สกัดของที่มีแพตเทิร์นชัดจากข้อความที่วาง — เบอร์โทร · อีเมล · เลขผู้เสียภาษี 13 หลัก · เงิน "12.5 ล้าน" → 12500000 · ปี พ.ศ.→ค.ศ.
- **จุดเด่น:** ฟรี · ทันที · **ทำงานออฟไลน์** (ไม่ต้องเน็ต/ไม่ต้อง AI) เสริมของที่ทำไว้ (source ข้อความคัดลอก)
- **มีบางส่วนแล้ว:** `buildPayload` แปลง พ.ศ.→ค.ศ. ของ date · parse `value_baht` ตัดคอมมา — **ยังไม่มี:** สกัดเบอร์/อีเมล/เลขภาษี + แปลงคำ "ล้าน"
- **กระทบโครงสร้าง:** ไม่ · แค่ util ใน `ai-intake.js` (ส่วนกลางเดิม) · **ขนาด:** S

### 3) P11 — Quick-log (ปุ่มบันทึกเร็ว)  → **ดีไซน์เต็มด้านล่าง**
- ตามที่เจ้าของขอ: ปุ่มแยกเฉพาะ ใช้ได้ทุกเครื่อง · สลับแท็บ Pending/Book 3 สี · ค้นหาของเดิม/เพิ่มใหม่ได้ในที่เดียว
- **กระทบโครงสร้าง:** ไม่ (ใช้ตาราง/adapter เดิมทั้งหมด) · **migration:** ไม่ต้อง · **ขนาด:** M

### 4) P7 + P8 — Telegram Alert + Weekly Digest
- **ทำอะไร:** เตือนเชิงรุก (งานใกล้ decision ≤7 วัน · next-doing เลยกำหนด · prospect ไม่มี log ≥14 วัน) + สรุปเช้าจันทร์
- **⚠️ ต้องตัดสินใจก่อน:** **ช่องทาง** — ทีมขายไทยส่วนใหญ่ใช้ LINE (LINE Notify ปิดแล้ว → ต้อง LINE Messaging API ซึ่งตั้งยุ่งกว่า) · Telegram ตั้งง่ายกว่าแต่ทีมต้องมีแอป
- **กระทบโครงสร้าง:** ต้องตั้ง server — Edge Function + cron (หรือ GitHub Actions schedule) · `BOT_TOKEN`/`CHAT_ID` เก็บใน secret ฝั่ง server **ห้ามลง repo**
- **ขนาด:** M (P7) + S (P8) · **ติดอะไรก่อน:** เคาะช่องทาง + เจ้าของตั้ง bot/secret เอง

### 5) P10 — G-Lead feed (tender leads)
- **ทำอะไร:** เมนู tender leads + import (วาง JSON/CSV) + กรอง/เรียง + ปุ่ม "ยกเป็น Pending"
- **⚠️ ต้องชัดก่อน:** ข้อมูล lead เข้ามายังไง — มี export จริงจาก G-Lead ไหม หรือวางเอง? ถ้าไม่มีแหล่ง = ตารางว่าง
- **กระทบโครงสร้าง:** เพิ่มตาราง `tender_leads` · **ขนาด:** M–L

---

## 🟥 ไม่ทำ (ทำแล้ว / ถูกถอด)
- **P1, P2, P4, P6** — ทำเสร็จแล้ว ทำซ้ำ = เสี่ยงพังของเดิม (โดยเฉพาะ P2 ที่ฝัง hex ชนกฎ print.css)
- **P3 Supplier** — เจ้าของสั่งถอดถาวร 23 ก.ค. · `project_suppliers` + "หา supplier จาก Pending" = ตัดถาวร **ห้ามเสนอกลับเองโดยไม่ถาม**

---

## 🎯 ดีไซน์เต็ม — P11 Quick-log (ปุ่มบันทึกเร็ว)

> โจทย์จากเจ้าของ (26 ก.ค. 2569): ปุ่มแยกเฉพาะ ใช้ได้บนมือถือ/iPad/laptop → สลับแท็บ **Pending / Book 3 สี** →
> ค้นหาชื่อลูกค้าเพื่อบันทึกต่อจากของเดิม (Book 3) · ถ้าลูกค้าใหม่กด **+ เพิ่มลูกค้าใหม่** ·
> หรือค้นหาโครงการเดิม (Pending) · ถ้างานใหม่กด **+ เพิ่มงานใหม่** ได้จาก Quick-log เลย

### แนวคิด
Quick-log = **ประตูบันทึกเร็วจุดเดียว** ที่ไม่ต้องเข้าไปหา record ก่อน — เปิดปุ๊บ เลือกปลายทาง ค้นหา/สร้างใหม่ แล้วลงบันทึกจบในหน้าจอเดียว

### ปุ่มเข้า (อยู่ตรงไหน) — ✅ เคาะแล้ว 26 ก.ค. 2569: **บนแถบหัว (topbar)**
- ปุ่ม **⚡ บันทึกเร็ว** บน **แถบหัว (topbar)** ข้าง ๆ ชิปชื่อผู้ใช้/ปุ่มธีม — โผล่ทุกขนาดจอ (มือถือ/iPad/laptop) เหมือนกันหมด เป็นปุ่มแยกเฉพาะตามที่ขอ
- ไม่ทำ FAB ลอย (เลี่ยงบทเรียน PWA bar ที่ของลอย `position:fixed` เคยบังปุ่มจนกดไม่โดน)
- ⚠️ แถบหัวตอนนี้มี: สถานะ · เวอร์ชัน · ชิปชื่อผู้ใช้ · ธีม · ออกจากระบบ (compact) → ใส่ ⚡ เพิ่มแล้ว `.topbar-right` wrap อยู่แล้ว ไม่ล้น (ทำ flex-wrap ไว้ตั้งแต่ v0.40)

### โครงหน้าจอ (2 สเต็ป)
```
สเต็ป 1 — เลือกปลายทาง + ค้นหา/สร้าง
┌──────────────────────────────────────┐
│ ⚡ บันทึกเร็ว                    [ปิด] │
│ ┌────────────┬───────────────┐        │
│ │ 🎨 Book 3 สี │ ▤ Pending    │  ← แท็บ │
│ └────────────┴───────────────┘        │
│ 🔍 [ ค้นหาชื่อลูกค้า / โครงการ… ]      │
│ ── ผลค้นหา ──                          │
│  ▸ คุณสมชาย · บ.เอบีซี · ล่าสุด 20 ก.ค.│
│  ▸ คุณสมหญิง · เทศบาลX · ล่าสุด 12 ก.ค.│
│ ── ไม่เจอที่ต้องการ? ──                 │
│  [ + เพิ่มลูกค้าใหม่ ]                  │  ← (แท็บ Pending = "+ เพิ่มงานใหม่")
└──────────────────────────────────────┘
        ↓ เลือก record หรือสร้างใหม่
สเต็ป 2 — ลงบันทึก (บันทึกต่อจากของเดิม)
┌──────────────────────────────────────┐
│ บันทึกติดตาม: คุณสมชาย · บ.เอบีซี  [←] │
│ วันที่ [26 ก.ค. 69]   ผู้บันทึก [ฉัน]  │
│ ช่องทาง: [โทร][เข้าพบ][ไลน์][อีเมล]…   │  ← ชิปเลือกเร็ว (ลงช่อง BY)
│ RESPONSE  [ ………………… ]                │
│ NEXT DOING[ ………………… ] วันที่[+7 วัน] │
│ 🤖 AI บันทึก (พิมพ์/เสียง/รูป)          │  ← ใช้ openAILog เดิม
│                       [ บันทึก ]        │
└──────────────────────────────────────┘
```

### กรณี "เพิ่มใหม่" จาก Quick-log — ✅ เคาะแล้ว 26 ก.ค. 2569: **ฟอร์มสั้น (เร็ว)**
- **ลูกค้าใหม่ (Book 3):** ฟอร์มสั้น — ชื่อ* + หน่วยงาน/บริษัท + ทีม + สี (ค่าเริ่มต้น 🔴 เพิ่งเริ่ม) → `saveCustomer` → เข้าสเต็ป 2 ลงบันทึกต่อทันที
- **งานใหม่ (Pending):** ฟอร์มสั้น — ชื่องาน* + ลูกค้า + มูลค่า + ทีม + ขั้นตอน(lead) + เดือนคาดปิด → `savePending` → เข้าสเต็ป 2
- "รายละเอียดที่เหลือ (37 ช่อง) ค่อยเปิดฟอร์มเต็มมากรอกทีหลัง" — Quick-log เน้นเร็ว ไม่บังคับกรอกครบ
  · มีลิงก์ "✎ เปิดฟอร์มเต็ม" ให้กดไปกรอกละเอียดได้ทันทีถ้าต้องการ

### ใช้โค้ดเดิมซ้ำ (ไม่สร้างซ้ำ)
| ส่วน | ใช้ของเดิม |
|---|---|
| ค้นหา | `adapter.listCustomers` / `listPending` (กรองฝั่ง client · ถ้าข้อมูลเยอะค่อยเพิ่ม search param) |
| ฟอร์มบันทึก | `logFormHtml`/`readLogForm` (loglist.js) |
| บันทึก | `adapter.addCustomerLog` / `addFollowLog` (RLS + created_by เป็นหลักฐาน) |
| สร้างใหม่ | `adapter.saveCustomer` / `savePending` (fillTeam เติมทีมให้เอง) |
| AI ช่วยบันทึก | `openAILog` (ตัวที่เพิ่งทำ v0.42) — ได้ text/voice/รูป ครบ |

### แบ่งเฟส
- **MVP (เฟส 1):** แท็บ + ค้นหา + บันทึกต่อของเดิม + เพิ่มใหม่แบบสั้น + ชิปช่องทาง + next-doing +7 · **ออนไลน์อย่างเดียว**
- **เฟส 2 (ทางเลือก):** ① สร้าง activity (B4) จาก next-doing อัตโนมัติ · ② **โหมดออฟไลน์**: คิวใน local → sync เมื่อออนไลน์ + badge "รอ sync"
  - ⚠️ ออฟไลน์คิวเป็นงานใหญ่ (IndexedDB queue + จัดการ conflict) — แยกทำทีหลังเมื่อ MVP นิ่ง

### สรุปผลกระทบ P11
- **กระทบโครงสร้าง:** ไม่ (ใช้ตาราง/adapter เดิม) · **migration:** ไม่ต้อง · **ติดอะไรก่อน:** ไม่มี
- **ราคาถ้าเลื่อน:** ถูกเท่าเดิม (เป็น UI ล้วน ไม่มีหนี้ schema) — เพิ่มเมื่อไหร่ก็ได้
- **ทับของเดิม:** ป็อปอัปบันทึกเร็วต่อ record (bqModal/quick log) ยังอยู่ได้ · Quick-log เป็น "ประตูรวม" ที่เพิ่มการค้นหา + สร้างใหม่เข้ามา

---

## หมายเหตุ
- ⚠️ ไฟล์ต้นทาง (ชุดคำสั่ง P1–P11) ที่เจ้าของส่งมา **ภาษาไทยเพี้ยนทั้งไฟล์ (mojibake)** — ตรงกับกับดัก `pbcopy` ไม่ใส่ `LC_CTYPE=UTF-8` ที่ CLAUDE.md เตือน · ถ้าจะเก็บไฟล์นั้น ควรสร้างใหม่ด้วย UTF-8
- ทุกข้อในไฟล์นี้ **ยังไม่ลงมือ** — เจ้าของเลือกก่อนเสมอ
