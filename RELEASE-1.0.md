# RELEASE-1.0.md — เช็กลิสต์เตรียมขึ้นเวอร์ชัน 1.0

> **1.0 = "ทีมใช้เป็นระบบหลักจริง + นิ่ง + ข้อมูลปลอดภัย"** ไม่ใช่ "ฟีเจอร์ครบ"
> โค้ดของโปรเจกต์นี้ทำเกินโรดแมปไปแล้ว (AI Import / AI บันทึก / Quick-log / ลบ log ฯลฯ)
> ดังนั้น 1.0 จึงวัดที่ **ความนิ่งและการใช้งานจริง** ไม่ใช่จำนวนฟีเจอร์
>
> เวอร์ชันปัจจุบัน: **v0.45.0** · repo: `te-sales/sales` · URL: <https://te-sales.github.io/sales/>
> อัปเดตล่าสุด: 26 ก.ค. 2569

---

## 🔴 เงื่อนไขที่ "ต้องผ่าน" ก่อนติดป้าย 1.0 (Hard gates)

### A. Setup ฝั่ง Supabase ครบ 100%
- [x] รัน migration ครบทุกไฟล์: `phase3-5` · `phase3-9` · `phase3-10` · `phase3-12` · `phase3-2` · `phase3-13` · re-run `policies.sql` — **ยืนยันแล้ว 26 ก.ค. 2569** (ผ่าน verification ทุกข้อ)
- [ ] ตั้ง **Site URL** = `https://te-sales.github.io/sales/`
- [ ] ตั้ง **Redirect URLs** = `https://te-sales.github.io/sales/**` (ไม่งั้นลิงก์ลืมรหัสผ่าน/ยืนยันอีเมลเด้งผิดที่)
- [ ] ปิด **public signup** (Authentication → Providers → Email → "Enable sign ups" = OFF)

### B. ทดสอบบนเครื่องจริง 3 เครื่อง (step 1.7 — step สุดท้ายในโรดแมป)
- [ ] **iPhone (Safari):** login · เพิ่มงาน · ดู dashboard · ติดตั้ง PWA (Add to Home Screen) · พิมพ์ฟอร์ม
- [ ] **Samsung S24 (Chrome):** เหมือนกันครบทุกข้อ
- [ ] **iPad:** เหมือนกันครบทุกข้อ (layout 2 คอลัมน์)
- [ ] ไม่มี JS error / หน้าไม่ค้าง / ปุ่มกดโดนทุกปุ่ม (โดยเฉพาะปุ่มแก้ไข ที่เคยโดนแถบ PWA บัง)
- [ ] เลขเวอร์ชันบนหน้าจอตรงกับที่ deploy (ถามทีมว่าเห็นเลขอะไร)

### C. บัญชีทีมครบ + ทดสอบ RLS ด้วย role จริง
- [ ] สร้างบัญชีครบทุก sale / manager (admin เชิญผ่าน Invite user)
- [ ] ผูก `team` + `team_access` ให้ถูกต้องทุกคน
- [ ] ทดสอบจริง: **sale เห็นเฉพาะงานทีมตัวเอง** (ล็อกอิน sale จริงแล้วดู)
- [ ] ทดสอบจริง: **manager เห็นตามสิทธิ์** (ดูทีมที่ได้รับ · ไม่เห็นทีมที่ไม่ได้รับ เช่น TE-IMP)
- [ ] ทดสอบจริง: **sale เซ็นรับทราบ/ลบลายเซ็นแทนหัวหน้าไม่ได้**
- [ ] ทดสอบจริง: **sale ลบ log ของคนอื่นไม่ได้ · ลบของตัวเองได้ · admin ลบได้หมด**

### D. ข้อมูลจริงเข้าระบบ + ทีมเริ่มใช้แทนของเก่า
- [ ] import/กรอกข้อมูลจริงเข้าระบบ (Pending + Book 3 สี)
- [ ] migrate นามบัตร 11 ราย (Obsidian, sale ธีระศักดิ์) เข้ามาครบ
- [ ] ทีม **เลิกใช้ Excel / prototype เดิม** มาใช้ตัวนี้เป็นระบบหลัก

### E. Backup พิสูจน์ครบวงจรแล้ว
- [ ] Export ทั้งระบบ → ได้ไฟล์ `te-backup-YYYY-MM-DD.json`
- [ ] ทดสอบ **import กลับ** ใน project ทดสอบ → ข้อมูลครบเหมือนเดิม (export → ลบ → import → ครบ)
- [ ] ตั้งรูทีน **export ทุกวันศุกร์** เก็บไว้นอกระบบ

---

## 🟢 สัญญาณว่า "พร้อมจริง" (Soft signals — ไม่ใช่ข้อบังคับ แต่ควรมี)
- [ ] ใช้งานต่อเนื่อง **~2–4 สัปดาห์** โดยไม่ต้องแก้บั๊กที่ทำงานไม่ได้ (blocker)
- [ ] ทีมกรอกข้อมูลเองได้ ไม่ต้องถามวิธีบ่อย
- [ ] ไม่มี "ของค้าง" ที่เป็น blocker ใน `autolog.md`

---

## ⚪ สิ่งที่ "ไม่ใช่" เงื่อนไข 1.0 (อย่ารอเกินจำเป็น)
- **ปิดยอด 80 ล้านบาท** — เป็น*ผลลัพธ์ธุรกิจ* ไม่ใช่เวอร์ชันซอฟต์แวร์ (1.0 = เครื่องมือพร้อมช่วยปิด ไม่ใช่ปิดได้แล้ว)
- **ฟีเจอร์ใน `UPDATE-PLAN.md`** (P7/P8 Telegram · P9 Win/Loss · P10 G-Lead · Quick-log ออฟไลน์) → เก็บไป **1.1, 1.2, …**
- **Edge Function AI** (มี fallback ก๊อปคำสั่งวางเองอยู่แล้ว ฟรี) — deploy เมื่อไหร่ก็ได้
- **แถบ Supplier** (พักไว้ตามที่เจ้าของสั่ง)

---

## ✅ เมื่อผ่าน Hard gate A–E + ใช้จริงนิ่ง ~2 สัปดาห์

1. **Tag เวอร์ชัน** — `git tag v1.0.0 && git push --tags` (bump `CONFIG.VERSION` = `1.0.0` + `sw.js` ด้วย)
2. **ใช้กติกาเวอร์ชันให้ชัด (semver):**
   - แก้บั๊ก → `1.0.1`, `1.0.2` (patch)
   - ฟีเจอร์ใหม่ → `1.1`, `1.2` (minor)
   - เปลี่ยนโครง DB → ต้องมี migration เสมอ (major/minor)
3. **1.0 = คำสัญญาว่า "จะไม่ทำข้อมูลทีมพังจากการเปลี่ยนโครง"** → ทุก breaking change ของ schema ต้องมีไฟล์ migration ใน `db/`

---

## 📊 สรุปสั้น
ขาดอยู่ **2 กลุ่มหลัก:**
1. **ตั้งค่า Supabase ให้ครบ** (Site URL / Redirect / ปิด signup) — SQL รันครบแล้ว ✓
2. **ทดสอบเครื่องจริง (1.7) + บัญชีทีม + ทีมใช้จริง + backup round-trip**

ผ่านสองกลุ่มนี้เมื่อไหร่ → ติดป้าย **v1.0.0** ได้อย่างมั่นใจ
